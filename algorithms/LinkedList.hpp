#pragma once

namespace LinkedList {

template <typename T>
T* ReverseSinglyLinkedList(T* head) {
    return head;
}

template <typename T>
T* ReverseDoublyLinkedList(T* head) {
    return head;
}

}
